export default function TestAdminPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">Test Admin Page</h1>
      <p>This is a simple test page to check if the app is working.</p>
      <div className="mt-4 p-4 bg-blue-100 rounded">
        <p>If you can see this page, the Next.js app is running correctly.</p>
        <p>The issue was likely with the authentication system conflicts.</p>
      </div>
    </div>
  );
}